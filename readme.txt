A genetic algorithm solution to the traveling salesperson problem.

The population size, number of cities, number of parents, and number of elites are modifiable by either: modifying the settings.txt file and using default settings when prompted; or passing no command line arguments and entering the values as prompted.
